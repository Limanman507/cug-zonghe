<view class="main-page">
  <map class="mapView" latitude="{{latitude}}" longitude="{{longitude}}" markers="{{markers}}" covers="{{covers}}" scale='18' show-scale enable-zoom	enable-scroll	 enable-traffic>
  </map>
  <view class="juli">距离我约：{{meters}}km</view>
  <button class="btn" type="primary" bindtap="openTgLocation">导航到店</button>
</view>